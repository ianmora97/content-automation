function loaded(){
    imageChange();
}

function imageChange(){
    // change between images every 5 seconds
    // var images = ["img/feature2.png", "img/feature3.png"];
    // var i = 0;
    // setInterval(function(){
    //     document.getElementById("imageCosy").src = images[i];
    //     i = (i + 1) % images.length;
    // }, 5000);

}

document.addEventListener('DOMContentLoaded', loaded);